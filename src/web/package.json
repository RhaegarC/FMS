{
  "name": "fms-web",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm -r --parallel build",
    "test": "pnpm -r --parallel test",
    "dev:admin": "pnpm --filter @fms/admin dev",
    "dev:user": "pnpm --filter @fms/user dev"
  }
}
