# Backlog — unspecced ideas

Ideas that are not yet split into a `docs/features/NN-name.md` feature file live here. When one
is prioritized, capture it with `/capture` (feature `NN`) to promote it into a numbered feature
file. The full PRD-derived plan lives in [00-mission-1-sprint.md](00-mission-1-sprint.md).

(empty — everything in the PRD is mapped in `00-mission-1-sprint.md`)
